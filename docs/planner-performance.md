# Planner performance benchmark

Generated by `pnpm tsx scripts/benchmark-planner.ts` on Node v22.23.1.

This benchmark exercises the **full bounded optimizer**, not only shortlist generation. The synthetic inventory keeps the frozen Lagos target/panel contract and expands only the site inventory.

| Inventory | Wall clock | Heap delta | Recommended sites |
|---:|---:|---:|---:|
| 50 | 486.0 ms | 10.3 MB | 6 |
| 100 | 360.8 ms | 15.7 MB | 6 |

## Guardrail

For the current deterministic demo engine, a 100-site run must complete within **2000 ms** and add no more than **256 MB** of heap in the benchmark process.

Result: **PASS**.

This is a regression guardrail, not a production SLA. Browser/device latency and larger real-world panels must be measured separately.
