name: Approve inspected UI59 Step 3 baseline

on:
  push:
    branches: [agent/ui-quality-hierarchy]

permissions:
  contents: write
  actions: read

jobs:
  approve-step3:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ui-quality-hierarchy
      - name: Download inspected CI failure artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api repos/GeoTracka/ooh-datahub/actions/artifacts/9088879231/zip > /tmp/ui59-step3.zip
          mkdir -p /tmp/ui59-step3
          unzip -q /tmp/ui59-step3.zip -d /tmp/ui59-step3
      - name: Approve only inspected Step 3 baseline and remove helper
        run: |
          cp /tmp/ui59-step3/test-results/visual-accessibility-locks-f3541-rchy-and-interaction-states-chromium/explorer-step3-actual.png \
            tests/e2e/visual-accessibility.spec.ts-snapshots/explorer-step3-chromium-linux.png
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tests/e2e/visual-accessibility.spec.ts-snapshots/explorer-step3-chromium-linux.png
          git rm .github/workflows/approve-ui59-step3.yml
          git commit -m "test: approve inspected desktop Step 3 baseline"
          git push origin HEAD:agent/ui-quality-hierarchy
