test: lock calibrated promotion wizard demo

Task 12 (final) of calibrated promotion wizard plan. Completes the
upload and enrichment vignette with UploadDialog (local spreadsheet
parsing, preflight-gated enrichment, geocode review, coordinate
correction, offline MapLibre + Google review scenes) and UploadPreview
numerical fallback. Replaces the placeholder upload shell in
PlannerPage with the real dialog and a context-status aside. Freezes
golden plan+RFQ bytes for all three sector presets with a reproducible
build script and byte-exact unit test. Adds DegradedClaimViews
component test covering all nine MetricClaim kinds. Adds five E2E
specs (seeded FMCG path, sector presets, upload enrichment,
network-disabled, visual accessibility), the client-secret verifier,
README, and demo comprehension check protocol. 139 tests passing.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
